apiVersion: v1
kind: Service
metadata:
  name: whoson
  namespace: whoson
  annotations:
    tailscale.com/hostname: whoson
spec:
  loadBalancerClass: tailscale
  type: LoadBalancer
  selector:
    app: whoson
  ports:
    - port: 80
      targetPort: 8080
